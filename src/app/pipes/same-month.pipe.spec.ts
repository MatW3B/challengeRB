import { SameMonthPipe } from './same-month.pipe';

describe('SameMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new SameMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
